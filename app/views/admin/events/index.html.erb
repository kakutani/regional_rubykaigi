<h1>Listing events</h1>

<%= link_to 'New event', new_admin_event_path %>

<table>
  <tr>
  </tr>

<% for event in @events %>
  <tr>
    <td><%= link_to h(event.title), edit_admin_event_path(event) %></td>
    <td><%= link_to 'Destroy', {:action => 'destroy', :id => event}, :confirm => 'Are you sure?', :method => :delete %></td>
	<td><%= link_to '参加登録者一覧', admin_event_attendees_path(event) %></td>
  </tr>
<% end %>
</table>

<br />
