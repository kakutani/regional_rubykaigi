%h1 Editing user

- form_for(@user, :url => {:action => 'update'}) do |f|
  = f.error_messages
  = render :partial => 'form', :locals => {:form => f}
  %p= f.submit "Update"

= link_to 'Back', admin_users_path
