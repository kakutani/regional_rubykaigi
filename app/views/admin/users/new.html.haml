%h1 New user

- form_for(@user, :url => {:action => 'create'}) do |f|
  = f.error_messages
  = render :partial => 'form', :locals => {:form => f}
  %p= f.submit "Create"

= link_to 'Back', admin_users_path
