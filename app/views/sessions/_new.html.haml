%h1 Log In
%p (アカウントがあらかじめ存在していないとログインできません)

- form_tag :controller => 'sessions', :action => 'create' do
  %p
    %label{:for => "openid_identifier"} OpenID:
    = text_field_tag "openid_identifier", @openid_identifier, :size => 50
    = submit_tag 'Log in'
